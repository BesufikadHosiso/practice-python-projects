fastapi>=0.111
uvicorn[standard]>=0.30
email-validator>=2.0
# Postgres back end (used when DATABASE_URL is set, e.g. on Vercel)
psycopg[binary]>=3.1
psycopg_pool>=3.2
# ASGI -> WSGI bridge for the @vercel/python runtime
asgiref>=3.7
# dev / test
pytest>=8.0
httpx>=0.27
sqlglot>=25.0
